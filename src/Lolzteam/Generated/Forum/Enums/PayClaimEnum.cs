// <auto-generated/>
#nullable enable

using System.Text.Json;
using System.Text.Json.Serialization;

namespace Lolzteam.Generated.Forum.Enums;

/// <summary>String-backed enum: PayClaimEnum</summary>
[JsonConverter(typeof(PayClaimEnumConverter))]
public enum PayClaimEnum
{
    /// <summary>now</summary>
    Now,

    /// <summary>later</summary>
    Later,

}

/// <summary>JSON converter for PayClaimEnum.</summary>
public sealed class PayClaimEnumConverter : JsonConverter<PayClaimEnum>
{
    private static readonly Dictionary<string, PayClaimEnum> s_fromString = new(StringComparer.OrdinalIgnoreCase)
    {
        { "now", PayClaimEnum.Now },
        { "later", PayClaimEnum.Later },
    };

    private static readonly Dictionary<PayClaimEnum, string> s_toString = new()
    {
        { PayClaimEnum.Now, "now" },
        { PayClaimEnum.Later, "later" },
    };

    public override PayClaimEnum Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        var value = reader.GetString();
        if (value != null && s_fromString.TryGetValue(value, out var result))
            return result;
        throw new JsonException($"Unknown PayClaimEnum value: {value}");
    }

    public override void Write(Utf8JsonWriter writer, PayClaimEnum value, JsonSerializerOptions options)
    {
        if (s_toString.TryGetValue(value, out var str))
            writer.WriteStringValue(str);
        else
            writer.WriteStringValue(value.ToString());
    }
}
